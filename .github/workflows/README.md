# GitHub Workflows

CI/CD workflow YAML files for GitHub Actions.

## What belongs here
- `ci.yml`, `cd.yml`, and `pr-checks.yml` workflow definitions
- Linting and compliance check pipelines
- Automated testing actions

## What does NOT belong here
- Local build scripts
- Local git hook configurations
