# Environment Configurations

Environment-specific configuration files for deployment targets.

## What belongs here
- `local.yaml`, `staging.yaml`, and `production.yaml` config files
- Environment variables maps
- Deployment-specific feature flags

## What does NOT belong here
- Database schema definitions
- Build scripts and tools
